% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agreement.R
\name{qda_units_binary}
\alias{qda_units_binary}
\title{The per-code binary view}
\usage{
qda_units_binary(
  fragments,
  code,
  unit = "annotationKey",
  coder = "codedBy",
  value = "code",
  uncoded = NULL
)
}
\arguments{
\item{fragments}{A fragments data frame from [qda_read_fragments()].}

\item{code}{The code to ask about.}

\item{unit}{Column identifying the unit of analysis.}

\item{coder}{Column identifying the coder.}

\item{value}{Column holding the category; `"code"` is the readable path,
`"codeId"` the identity that survives renaming.}

\item{uncoded}{Optionally the matching uncoded export, so segments no
coder coded become their own category.}
}
\value{
A character matrix as in [qda_units()], with values `"yes"` and
  `"no"`.
}
\description{
Turns one code into a yes/no judgement per unit, which is how a
multiply-coded body of material can still be assessed: every code is asked
about separately, and a segment carrying three codes contributes to all
three questions instead of being dropped.
}
\examples{
frag <- data.frame(
  annotationKey = c("s1", "s1", "s2", "s2"),
  codedBy = c("ann", "bob", "ann", "bob"),
  code = c("A", "A", "B", "A")
)
qda_units_binary(frag, "A")
}
