% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract.R
\name{qda_contract}
\alias{qda_contract}
\title{The zotQDA exchange contract}
\usage{
qda_contract(path = NULL)
}
\arguments{
\item{path}{Optional path to an `exchange-v*.json`.  Defaults to the copy
shipped with this package.}
}
\value{
A list with `contract`, `version`, `csv` and `formats`.
}
\description{
zotQDA and qdaZ write versioned files described by a machine-readable
contract.  Every file states its kind and version in its first column, so
a reader can recognise what it is holding without relying on the file name
and can refuse a version it does not understand instead of quietly
computing something wrong.
}
\examples{
ct <- qda_contract()
ct$version
names(ct$formats)
}
