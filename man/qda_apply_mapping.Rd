% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{qda_apply_mapping}
\alias{qda_apply_mapping}
\title{Apply a consensus mapping to coded fragments}
\usage{
qda_apply_mapping(fragments, mapping, coder_col = "codedBy")
}
\arguments{
\item{fragments}{A fragments data frame.}

\item{mapping}{A consensus-mapping data frame.}

\item{coder_col}{Column holding the coder in `fragments`; defaults to
`"codedBy"`.}
}
\value{
`fragments` with an added `consensusCode` column.
}
\description{
Adds a `consensusCode` column without touching `code`: the original
coding stays visible next to its consensus interpretation.
}
\examples{
frag <- qda_read_fragments(qda_example("zotqda-fragments.csv"))
map <- qda_read_mapping(qda_example("zotqda-konsens-abbildung.csv"))
out <- qda_apply_mapping(frag, map)
names(out)[ncol(out)]
}
