% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{qda_agreement_by_code}
\alias{qda_agreement_by_code}
\title{Agreement per code}
\usage{
qda_agreement_by_code(
  fragments,
  min_n = 3,
  unit = "annotationKey",
  coder = "codedBy",
  value = "code"
)
}
\arguments{
\item{fragments}{A fragments data frame from [qda_read_fragments()].}

\item{min_n}{Skip codes used fewer than this many times; with two or three
uses every coefficient is noise.}

\item{unit}{Column identifying the unit of analysis.}

\item{coder}{Column identifying the coder.}

\item{value}{Column holding the category; `"code"` is the readable path,
`"codeId"` the identity that survives renaming.}
}
\value{
A data frame with one row per code: `n` uses, `units` compared,
  `percent`, `cohen`, `ac1`, and `prevalence` with its Wilson interval.
}
\description{
A single pooled coefficient hides which codes the coders actually argued
about.  This asks the question once per code, as a yes/no judgement, which
is also the only honest way to treat material where segments legitimately
carry several codes.
}
\examples{
frag <- data.frame(
  annotationKey = rep(paste0("s", 1:6), each = 2),
  codedBy = rep(c("ann", "bob"), 6),
  code = c("A", "A", "A", "B", "B", "B", "A", "A", "B", "B", "A", "A")
)
qda_agreement_by_code(frag, min_n = 1)
}
